#!/usr/bin/env bash
# Monitoring component env overrides
# Sources parent configs in deploy.sh; add component-specific overrides here.
export ACCOUNT_NAME="eci-dev"
export PROMETHEUS_RETENTION="15d"
export GRAFANA_ADMIN_PASSWORD_SECRET="grafana-admin-secret"   # K8s secret name
